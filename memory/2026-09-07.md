# 2026-09-07 — AI Tools Content Daily

## 产出 (3 articles, all Silver+, 07:07 cron 触发)

1. **codenotch Review 2026** (reviews, 7.4/10, body 1381 词) — vinzdg/codenotch 679★/90f (09-05 创建，2 天内)，MIT，Swift/macOS。
   - 定位：屏幕边缘小黑 notch，常驻显示 Claude Code/Cursor/Codex/Antigravity/GLM 的用量限额烧了多少——"哪个 agent 现在还有额度？"的 ambient 答案；永不 sign-in，每个 adapter 借宿主工具已有的凭据/会话（Claude OAuth keychain token 打自家 /usage 同款 endpoint、Cursor 本地 SQLite、Codex app server、Antigravity language server、GLM Z.ai monitor endpoint）
   - Fidelity 三档（.official/.derived/.manual）+ 显式降级（stale/needsAuth/error 不造假数字）+ ring 永远跟 Claude /usage 的 session window 对齐（"一个数只表达一个意思"）+ derived 数字带 "~" 前缀
   - "is it still working?"：busy 转圈 arc / 等你时 amber pulse，hover 显示每个 live session 名字/位置/诉求
   - 多登录：CLAUDE_CONFIG_DIR=~/.claude-work → 独立 ring；任意 ~/.claude-<slug> 启动即发现
   - 工程细节：usable edge 吸附（bottom notch 跟 Dock 走）、硬件 notch 顶部同形、429 地板抬升退避（60s 翻倍封顶 15min + deadline 持久化）、keychain 用 item modification date 绕过访问提示、轮询空闲 5min、Sparkle EdDSA 签名自更新、CODENOTCH_DEMO=1
   - 诚实面：README+spec 明说名字是 placeholder、无 vendor 公开 clean usage API、adapter endpoint 随时变；TASKS.md 开放决策（名字/ceilings 手配还是推断/是否做 web-session adapters）
   - 边界：macOS only（Windows port issue #7 待 merge）、2 天龄 12 commits/5 contributors、单设备无同步、observer 非 manager（不能 pause/route/enforce）
   - 角度：vs honey-for-devs（站内 06-26，降 token 消耗的 skill）——codenotch 是 meter 不是 reducer；站内 0 覆盖（grep 验证）
   - 背景：09-06 记忆里该 repo 曾被排除（当时 207★ 1 天龄无 license），现 MIT + 677★ 翻盘入选

2. **ffmpeg-skill Review 2026** (reviews, 7.7/10, body 1283 词) — kajisho5/ffmpeg-skill 320★ (09-03 创建)，MIT，Python 3.9 stdlib + FFmpeg 5.0+，v0.10.0。
   - 定位：Agent Skill 教 Claude Code/Cursor/Codex 走 probe → edit losslessly → check → verify 固定流程，21 个结构化 tool 做实事；agent "knows FFmpeg" 仍靠猜 → 用测量+验证消除猜
   - 21 tools 五组：analysis（probe/scenes/look）、editing（cut/join/silence/fit）、audio（audio/sync/loudness）、picture（caption/overlay/graphics/color）、delivery+orchestration（export/check/report/render/batch/multicam/verify）
   - 设计原则 8 条：probe first / lossless when possible / plan before render（--dry-run 全 tool 测试在 fake ffmpeg 后断言零调用）/ machine-readable contract（从 argparse 生成非手维护）/ contract-derived MCP（tools/list 与脚本 byte-identical 有测试钉住）/ capability detection（doctor 读 -encoders/-filters/-bsfs）/ unknown is not missing / verify result（look.py contact sheet 靠 agent 自己眼睛）
   - 诚实边界：look.py 只渲染 PNG，"inspects" = 调用方 agent 的 vision；scenes --highlights 用 proxy（audio/duration）非内容；fit 默认中心裁切；sync.py 只 audio-to-audio 无 lip-sync
   - 实测数字：92/92 verification（10 文件真实设备语料 GoPro/DJI/iPhone Dolby Vision/HDR10/24p）、sync 40/40 within 10ms（max err 1.1ms）、silence 0 missed gaps、scenes F1 0.97、72/72 分级 agent runs（24 prompts 英/日含必须拒绝 4 条，独立模型打分）
   - FFmpeg 8 兼容坑自曝：0.9.0 flag 列变短全 filter 误报 missing → 0.9.1 改认 io-spec token（A->A 等）；CI 上传各 runner listing 当 fixture
   - 生态：kajisho5 全家桶的 "hands"，brain 在前（video-production-agent/AI-video-production-OS 读它的 contract），依赖单向
   - 角度：站内视频工具全是生成/剪辑 SaaS（descript/runway/veo）——agent 本地 FFmpeg 编辑 skill 品类 0 覆盖

3. **Choruz Review 2026** (reviews, 7.0/10, body 1245 词) — inclusionAI/Choruz 305★ (09-02 创建 GitHub)，MIT，Rust + Next.js + Postgres 16，v0.1.0 dev preview。
   - 定位：local-first Slack-like 人+AI agent 协作空间，但每个 agent 是**真实 CLI**（Claude Code/Codex/Pi/Grok/OpenCode/webhook）跑在自己 workspace/git worktree——不 sandbox 仿真，保底层 harness 全部能力
   - Agent protocol 全文档化：`[choruz-incoming] from:@<> group:<> conv:<id13>[ thread:<>] roster:[…][ your_tasks:[…]] | content` envelope；出站 = .choruz/send helper 写 Maildir outbox（tmp/→lock→seq→new/），命令类型 send/share_file/provision_agent/create_group/set_cron/task_create|update|transfer；CLAUDE.md/AGENTS.md 由版本化模板渲染（bootstrap-version marker + choruz-protocol: v3-maildir）
   - Runtime：binding = agent principal × driver_type × workspace；PTY terminal（direct chat）或 headless turn（group）；harness accounts 浏览器 sign-in（Claude/Codex）；native session 可导入；agent_turn_leases 租约 + WAL 恢复；share_file 出 workspace symlink 拒绝 + 8MiB 上限
   - Pipeline：event-sourced Postgres（conversation_events seq + event_outbox + CDC poller → router → NOTIFY dispatch lease → executor headless turn → outbox drained → writer commit by turn_id）；幂等 = client_msg_id/turn_id 两个 partial unique index；per-device sync cursor（sync_device.ack_cursor）；web client IndexedDB upsert by [conv_id, server_seq]
   - 架构：modular monolith Cargo workspace + choruz-api-gateway（唯一 Rust service，/v1 全路由 + ws sync + terminal）+ choruz-pipeline worker + Next.js web；远程加 choruz-server/choruz-connector/Cloudflare Worker relay；每子系统一页 owns/data/entry-points/invariants/failure-modes/tests
   - 诚实面：v0.1.0 明说 interface/data format 可 incompatibly 变 + offline conversion guide；四进程本地栈（PG16+Rust+Node24+pnpm10）无 hosted trial；Gemini driver 加了又禁（3 个 migration）；37+ migrations、data-model doc 日期 2026-03-29（开源前已开发数月）
   - 角度：vs 托管 agent workspace（chatgpt-work/kimi-work，黑盒）vs 裸终端多 agent——补 persistence/structure/human surface；站内 0 覆盖（grep 验证）

## 流程备注

- 选题来源：GitHub search API 多轮（created>09-05 stars>80 / created>09-03 stars>100+ / 语言过滤 / pushed>09-05 active 过滤 / topic:claude / mcp-server / claude-skill）+ HN front page + Show HN 48h
- **今日 GitHub 井枯**：大量非 AI（DLSS 家族/DLSS5-Swapper/stop-stutter/macos-sysdata）、中文生态（wechat-intelligence-hub/easy-writing/guizang-yingzao/oc-english）、spam 集群（fable-orchestrator/codex-astra-luna-orchestrator/usque-custom-pro/warp-masque-actions 同构号群）、scam（TRX-Drainer/Solana-Drainer/undress-service/meme launchpad）、star-farm 信号（pierrenade/short-video-generator-AI：12 commits 全在创建后 17 分钟内、此后零 push、1 contributor、809★——排除）
- 候选排除：okf-agent-memory（344★ Go OKF v0.2——**agent-memory 品类 09-06 刚做过 tigerless 版**，连续两天同品类伤站内多样性，明确跳过）；human-atlas（3D 解剖非 AI 工具，09-06 已排）；Wiggle（69★ 1 天龄声称复刻 Anthropic sandbox VM，provenance 存疑）；Penelopa.ai（71★ 营销味重闭源 SaaS 安装器）；wechat-intelligence-hub（中文微信生态，09-06 已排）
- 三个选题均站内 0 覆盖（grep 全 src/content 验证：codenotch 0 / ffmpeg-skill+kajisho5 0 / choruz 0）
- 截图脚本 scripts/screenshots-2026-09-07.mjs 全部 proxy 成功（3 张 1440x900 ✅，156-200KB，PIL 亮度/方差验证非空白，GitHub repo 页）
- 无 CJK、无 secret pattern（hf_/AKIA/ghp_/sk-/AIza 全部 0 命中）
- 本地 build ✅ 823 pages（820 + 3 新增），frontmatter 13 字段全齐
- GitHub API 裸 curl 限流 → 切 gh CLI（wuledan token）
- 质量自评：三篇均 Silver+；codenotch 有 fidelity/退避/多登录细节 + 翻盘故事线；ffmpeg-skill 有 21 tool 分组 + 8 设计原则 + 实测数字 + FFmpeg8 自曝回归；Choruz 有 agent protocol 原文级细节 + event-sourced 幂等/租约 + dev-preview 诚实面
- 今日未写 memory 前 git 状态干净（昨天 5b60d28 已 push）

## ⚠️ 小七通报待补发 (07:35 CST)

- sessions_send agentId=main 失败：`FallbackSummaryError: All models failed (4): atlas-codex/gpt-5.4: 503 No available channel | open-code/deepseek-v4-pro: 429 Monthly usage limit reached` — 与 09-06 相同：小七 main session 模型通道故障（目标侧 infra，非本 session 问题），连续第二天
- **待办**：下次会话/心跳/复盘发现本文件此节时，重发以下日报给 agentId=main；若 gpt-5.4 恢复则直接成功

补发内容：
📋 AI Tools 内容产线日报 (2026-09-07)
✅ 完成：3 篇 Silver+ 文章已发布至 toolsdepth.com
1. codenotch Review 2026 (reviews, 7.4/10, 1381词) — vinzdg/codenotch 679★/90f (09-05创建2天), MIT Swift/macOS。屏幕边缘notch常驻显示Claude Code/Cursor/Codex/Antigravity/GLM用量限额，永不sign-in借宿主凭据，Fidelity三档+429退避+多登录多ring。站内usage-monitor品类0覆盖（honey-for-devs是降耗skill不同）。
2. ffmpeg-skill Review 2026 (reviews, 7.7/10, 1283词) — kajisho5/ffmpeg-skill 320★ (09-03创建), MIT Python+FFmpeg5。21个结构化工具教Claude Code/Cursor/Codex走probe→lossless edit→verify流程，contract从argparse生成+MCP零漂移，92/92真实设备语料+72/72分级agent runs。站内agent本地视频编辑品类0覆盖。
3. Choruz Review 2026 (reviews, 7.0/10, 1245词) — inclusionAI/Choruz 305★ (09-02创建), MIT Rust+Next.js+PG16, v0.1.0。local-first Slack-like人+AI agent协作，每agent跑真实CLI（Claude Code/Codex/Pi/Grok/OpenCode）于自有workspace，Maildir outbox agent protocol + event-sourced Postgres pipeline。
✅ 质量门禁全过：3张真实截图1440x900 (156-200KB) / 无CJK / 无secret / build 823页 (820+3)
✅ Git: 57730d4 pushed (3a37f71..57730d4)
✅ Vercel Production Ready，toolsdepth.com 三篇均 HTTP 200，标题内容确认
⚠️ 备注：今日GitHub井枯——大量spam集群/star-farm（short-video-generator-AI 12 commits全在17分钟内+死号809★已识别排除）/非AI/中文生态；okf-agent-memory因agent-memory品类昨日刚做过主动跳过
无阻塞。

## 阻塞/风险

- 无阻塞。提示：codenotch 名字是 placeholder + macOS only + adapter 依赖 vendor 私有 endpoint；ffmpeg-skill 要 ffmpeg-full（Homebrew 普通版缺 libass/drawtext/zscale）+ 强多模态 agent 做视觉判断；Choruz v0.1.0 dev preview 四进程栈 + 数据格式可 incompatibly 变（升级要 offline conversion）+ 无 hosted trial
