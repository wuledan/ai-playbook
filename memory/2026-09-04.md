# 2026-09-04 — AI Tools Content Daily

## 产出 (3 articles, all Silver+, 07:07 cron 触发)

1. **reverify Review 2026** (reviews, 7.5/10, body 1332 词) — 2akouwu/reverify 747★/152f（08-31 创建，4 天内），MIT，Python。
   - 定位："The AI proposes. The bytes decide." — AI 逆向工程谎言检测器：LLM 读二进制必幻觉（offset/struct/行为），reverify 用确定性纯 Python RE 工具箱当裁判，模型提议 → 工具对照真实字节验证 → VERIFIED/REFUTED/INCONCLUSIVE + 证据字节
   - 基准可复现：19 个真实 Windows 系统文件上 AI textbook 答案 100% 错误、reverify 全抓且零误报（BENCHMARK.md / EXAMPLE.md kernel32.dll 全程无 API key、模型无关）
   - 验证循环细节：claim kinds（bytes_at/typed reads/pattern/instructions/emulate_result/protobuf_field/imports…）、file/RVA/VA 三地址回显、observe 模式（读而非断言）、depends_on 级联失效、--claims-file 批量、refuted 非零退出 → agent/CI 门禁
   - Grounded = informative 反游戏设计：信息权重从二进制实测（频次×熵），复述 fact sheet/回声工具输出/普遍 prologue 权重≈0（CORE 版 FActScore 精化）；reconstruct --samples N 由 verifier 而非模型自信度选
   - v0.8.0 ledger：.reverify/ledger/<sha256>.json 每轮 checkpoint（content-keyed），/clear、auto-compact、重启后无损恢复；refutation 以 KNOWN FALSE 回归（负记忆）；kernel32 43k chars 事实表塞进 20k prompt budget；Claude Code SessionStart hook + MCP re_ledger
   - 版本史即架构史（4 天 v0.3→v0.8）：v0.3 capstone/unicorn/lief 可选就位升级；v0.4 反游戏加固 + testbed 交叉校验（纯 parser vs lief、反汇编/模拟器 vs capstone/Unicorn、known-answer、fuzzing，196 单测）；v0.5 behavior_equiv（原函数 vs 候选同输入对比、mismatch 返反例，ExeBench/LLM4Decompile 方法）；v0.6 两阶段 observe-then-hypothesize；v0.7 prove_equiv Z3 全输入证明（MBA 混淆，proven > tested > observed 强度阶梯）
   - MCP 原生：re_verify_claim/re_ledger 供 Claude Code/Cursor 直调；CLI 全套（auto/parse/disasm/pattern-scan/strings/emulate/decode-protobuf/decode-tlv/gen-hook/audit-boundary）
   - 诚实边界：4 天龄单人维护（22 commits/1 watcher）、版本 API 可能漂移、只验证 claim kinds 能表达的假设（自由叙述不可信）、价值随接入的 proposer 模型走、纯授权 RE 定位（SECURITY.md）
   - 角度：vs "让模型更谨慎"（训 prompt）、Ghidra/Binary Ninja AI 插件（闭源无验证循环）

2. **reef Review 2026** (reviews, 7.5/10, body 1037 词) — Human-Agent-Society/reef 272★/22f（08-31 创建，4 天内），Apache-2.0，Python，reefinfra.ai/docs。
   - 定位：self-improving agents 的持续学习后端 — 像装 codex/opencode 一样 curl 装 agent harness，把模型请求指向 reef 的 inference endpoint（OpenAI + Anthropic 兼容：/v1/chat/completions 和 /v1/messages），后端持续评估行为并改进 harness + 权重
   - 四步循环：Serve（service/+runtime/）→ Observe（records.py + train/processors feedback matching & eligibility）→ Grow（recipe/ + train/ batches & update jobs）→ Commit（train/evaluation 候选评估 + artifact/ 版本史 + surface/ 交付）
   - API 细节：x-reef-scenario header 建 scenario（绑 recipe，请求不选 recipe）；响应带 x-reef-agent-record-id receipt；/reef/report 收 score（数值）+ feedback（文本/结构化）+ references（receipts），schema 校验（reef/core/reports/）；够数后训练步热切换权重不重启
   - 两个学习面：权重（SGLang 推理 + THUDM slime 训练）和 harness 树（rules/skills/config/prompts/extensions，cordis 演进；候选 vs 当前在任务上评估、赢了才发布；scenario 间不共享数据/版本）
   - 安装流：curl /reef/harness/install?adapter=pi | bash → reef-pi -p "fix the bug"；reef-pi report --score/--feedback 自动带 receipts；版本检查：交互 "Update with… / Skip"、headless 打印指令；x-reef-scenario header 取演进版 harness
   - Cookbook recipes 表：SAORecipe（测试/verifier 打分的任务流 → 权重，arXiv:2607.07508）；OpenClawRLRecipe（agent 流量无显式 report、用 next-state 信号 → 权重，**OpenClaw**）；TTTDRecipe（重复计分尝试 → 权重 + guidance-model 变体冻结 executor）；SkillClawRecipe（agent feedback → skill pool 演进，**无 GPU**）
   - 差异表：vs 推理引擎（vLLM/SGLang serve 不 train）、RL 框架（slime/veRL/AReaL train 不 serve live）— reef 补中间：serve ✓ train ✓ 版本管理 ✓ 热更新 ✓ 超越权重演进 skills/harness ✓
   - 诚实边界：4 天龄 50 open issues（Codex adapter 09-03 合并、Terminus 2 PR、hosted sandbox #205 提案，面在周级变动）；权重训练 recipe 要 GPU + git-lfs + 源码 checkout（recipes 不进 wheel）；反馈信号全靠自己 workload 产；eligibility 机制微妙需读 docs；无 HN/Reddit，社区在 Discord/X
   - 10+ contributors（top3: 47/32/10 commits）= 研究组在认真 shipping
   - 角度：vs 自拼 vLLM+slime 管道（无版本管理无 harness 面）、托管 agent 平台（黑盒）

3. **m3e-canvas Review 2026** (reviews, 7.3/10, body 1028 词) — lnkiai/m3e-canvas 459★/27f（09-02 创建，2 天内），MIT，Next.js 16 + React 19，无后端。
   - 定位：浏览器里画 Material 3 Expressive 屏幕 → 连成 flow → 导出自然语言 prompt 给 AI 编码工具（vibe coding 的 brief 环节）；README demo：编辑器画的 habit-tracker 被 AI 工具生成并在 Android 上跑起来
   - 部件库是真 M3E 不是圆角矩形：buttons/icon/FAB/split/FAB menu/chips/app bars/nav bars/floating toolbars/tabs/search bars/cards/lists/dialogs/snackbars/text fields/switches/sliders…含 material-components-android 移植的 shape-morphing loading indicator
   - 交互内建非描述：tap 目标 + slide 四向/fade/expand/none 转场（back 逆播）、swipe 四向开屏（预览跟手）、toggle 按钮点击翻转 icon/样式；preview 可点按验证 flow
   - 磁性连接（部件靠近熔成 group 圆角软化）+ 显式布局描述（prompt 点名 overlaps/横排）解决 vibe-coding 布局崩坏经典问题；layers/groups 保 z-order
   - 主题面板四轴：color（7 preset 或 seed → 全 M3 scheme、明暗、3 对比度、dynamic color）、shape（全部件 square/rounded/full）、type（Roboto/Flex/Serif/system + emphasized）、motion（standard/expressive spring 且驱动 preview）
   - Prompt 导出：整体或单屏、EN/JA/ZH 三语、带每部件行为笔记；copy 或 PNG 导出；localStorage 零后端零登录；手机端 buttons-only 单屏编辑器；GitHub Pages live demo
   - 诚实边界：只认 M3E 一种设计语言（iOS/web 非 Material 不适用）；2 天龄单人维护 0 issues（是没时间出 issue 不是没 bug）；自己不生成任何东西（prompt 质量跟 AI 工具走）；全功能编辑器仅桌面浏览器；localStorage 无分享/跨机版本/可编辑导出；无 HN/Reddit 无三方内容
   - 生态钩子：matraic/m3e（M3E Lit web components + React bindings）是 README 指明的"sketch 后归宿"，sketch in m3e-canvas → build with m3e 是完整工作流
   - 角度：vs Figma→AI 插件（通用但无 M3E 语义无 prompt 管线）、vs 纯文字描述给 AI（布局/间距/组件靠脑补）

## 流程备注

- 选题来源：GitHub search API（created:>2026-08-30 stars:>40 两档）+ HN front page（今日无强 AI 工具帖：GPT-6 Astra/Qwen 3.8 27B Cerebras 模型新闻为主，K2 Horizon 是模型族非工具）
- 候选排除：gesta-run/subpool（31★ 太薄）、FalconFlank（CVE 利用非工具）、fable51-worlds/fable-orchestrator（游戏引擎生态）、skill-cabinet（09-02 已覆盖）、easy-writing（222★ 但 Vue 中文网文写作，品类可后续考虑）、image-to-3d-pipeline（242★，站内 Meshy 3D 已覆盖近邻品类，机制不同但素材厚度不如 m3e-canvas）、ai-evaluation-framework（112★ 1 天龄太薄）
- 三个选题均站内 0 覆盖（grep 全 src/content 验证；"reef" 唯一命中是 build-custom-gpt 里的 "freeform" 子串，误报；m3e-canvas 0；reverify 0）
- 截图脚本 scripts/screenshots-2026-09-04.mjs 全部 proxy 成功（3 张 1440x900 ✅，190-217KB，PIL 亮度/方差验证非空白，GitHub repo 页）
- 无 CJK、无 secret pattern（hf_/AKIA/ghp_/sk- 全部 0 命中）
- 本地 build ✅ 817 pages（上次 814 + 3 新增）
- GitHub API 裸 curl 限流 → 切 gh CLI（wuledan token）拿 commits/contributors/issues 数据
- Commit: `f09fd65` content: AI Tools daily 2026-09-04 — 8 文件干净提交（3 md + 3 png + 1 script + 1 memory），push 直连成功（51a9fcb..f09fd65）
- Vercel: 待确认（push 后 ~2 分钟）

## 质量自评

- reverify: Silver+（基准 100% 抓幻觉零误报 + 验证循环全细节 + 信息权重反游戏设计（FActScore CORE）+ v0.3→v0.8 四天版本史即架构史 + ledger 无损上下文机制 + MCP/CLI 双面 + 152 forks 高 fork 率信号 + 单人/4 天龄诚实边界 + 与"训 prompt 更谨慎"/Ghidra AI 插件对比）
- reef: Silver+（四步循环模块级拆解 + receipt/report API 细节 + 双学习面（权重/harness）+ curl 安装流 + recipes 表逐行（含 OpenClawRL/SkillClaw 无 GPU）+ 差异表三列对比 + 50 issues/10+ contributors 活跃度 + GPU/git-lfs/反馈信号自备等操作层诚实边界）
- m3e-canvas: Silver+（editor→prompt→Android 实证 + 部件库/M3E loading 移植细节 + 磁性连接/转场/主题四轴 + 布局显式描述解决 vibe-coding 崩坏 + 三语 prompt 管线 + matraic/m3e 生态钩子 + 2 天龄/单语言/不生成等边界 + 与 Figma AI/文字描述对比）

## 阻塞/风险

- 无阻塞。提示：reverify 单人快速迭代 API 可能漂移、只保证 tool-checkable 假设；reef 权重训练要 GPU 且 recipes 不进 wheel（要源码 checkout）；m3e-canvas 若目标是 iOS/web 非 M3E 场景不适用
