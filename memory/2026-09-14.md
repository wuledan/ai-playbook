# 2026-09-14 — AI Tools Content Daily

## 产出 (3 articles, all Silver+)

1. **sol-pi Review 2026** (reviews, AI Development, 7.6/10, body 1102 词) — NVlabs/SoL-Pi 1702★/127f (09-02 创建, pushed 09-14), MIT, TypeScript, 38 open issues。定位：Pi coding-agent harness 的独立扩展，把 auto-research 搜出的 4 个机制打包成 token 效率层。
   - 4 机制：Action Fusion(tools, edit+验证命令合一)、ObservationPack(observations, 大输出→稳定 handle + obs_recall 分页召回)、Evidence-Preserving Reducer(delegation, 长日志→receipts 且引文必须与归档源一致)、Online Context Compact(context, 完成计划步→native compaction 候选点，压缩后新 turn 续跑)
   - 4 规则：no Pi patches / explicit opt-in(全默认关) / preserve evidence / 用 Pi 的运行时选择
   - 研究故事：152 提案 → 4 存活；EdgeBench 51 任务 held-out 套件
   - 经济性：省 $8.75–$13.50/hr vs native Codex/Claude Code；$4.36–$5.71/hr vs Pi（官方 API-equivalent 计价）
   - 配置：单个 sol-pi.json（4 布尔 + reducer provider/model + cacheWriteReadRatio 默认 12.5），项目级>用户级不合并；preflight check-sol-pi-config.mjs；凭证不入文件
   - 安装：Node 22.19+ / @earendil-works/pi-coding-agent@0.84.2 / `pi install git:github.com/NVlabs/SoL-Pi`
   - 存储：<session-dir>/sol-pi/<session-id>/{observation-pack,evidence-preserving-reducer}，归档不自动删
   - 诚实面：Pi-only、2 周龄、38 自报 issue（Windows Git Bash 路径 / obs_recall 部分字符 / ObservationPack 每投影重走 payload / OCC 不读 plan progress）、数字自报且 EdgeBench 未公开 leaderboard、reducer 会花模型调用且失败即 no-op、归档累积

2. **tokentab Review 2026** (reviews, Developer Tools, 7.3/10, body 1007 词) — crwdla/tokentab 852★/212f (09-07 创建 17:18 单 commit 17:20), MIT, Python, 0 issues。定位：本地只读 CLI + web dashboard，把 Claude Code/Codex/Gemini CLI 已写的 session log 换算成 token 与美元。
   - 读：~/.claude/projects/**/*.jsonl / ~/.codex/sessions/**/rollout-*.jsonl / ~/.gemini/tmp/**/session-*.json；Cursor 是 stub
   - CLI: -today/-month/-p all/--provider/--project/--from/--to/--json/-web；管道自动去色
   - dashboard: localhost:4747，stdlib http.server 无框架，每请求读盘不缓存，仅绑 localhost，不拉 CDN 字体
   - 计价：手维护表 tokentab/pricing/prices.py（$/1M），模糊匹配（claude-opus-4-6-20260514→claude-opus-4-6），未匹配=$0.00 且 CLI 明说
   - 缓存正确性：Claude 分 cache read/write，Gemini input 含 cached → 计价前扣掉 cached 避免双计
   - 依赖：仅 rich；其余 stdlib；provider 单模块 collect()→UsageRecord
   - 读法建议：cache hit <80% 持续=上下文不稳；大模型打小调用=该换便宜模型；chat/exploring 占比高=说话读文件多
   - 诚实面：1 周龄且单 commit（创建/推送差 2 分钟）、Cursor 未完成、价格 best-effort、activity 是确定性启发式"hint not gospel"、0 issue 含混、README 有 `python cli.pyb` typo

3. **anything2explainer Review 2026** (reviews, Video, 7.8/10, body 1010 词) — Vincentwei1021/anything2explainer 1171★/207f (09-08 创建, pushed 09-13), PolyForm Noncommercial, TS, 18 topics, 0 issues。定位：Claude Code/Codex skill，主题→黑底 MG 讲解视频（Remotion 代码绘制，TTS 配音+字幕+章节进度条，中英双语）。
   - 输出：1280x720@30 H.264 + 配音 + word-boundary 字幕 + 章节卡 + 顶部 HUD + 底部进度条 + 全程纸质轨迹（带源链接调研/解说/分镜/每镜头源码/QC 报告）
   - 9 阶段流程（SKILL.md）：scaffold→research(1 agent,每数字带 URL)→narration+voiced timeline→storyboard→overlays/primitives→pilot 30s→并行 build(每组 5-7 镜头)→render+frame_metrics→QC/fix
   - 4 确认点：时长语言(先于文案)、解说定稿(先于配音)、TTS 引擎、前 30 秒(先于其余组)
   - 时长档：2-3min=24-32 镜头/4-6 agent/≈1h；3-5min=40-50/8/≈2h；5-8min=60-80/10-14/≈2-3h + ~3GB
   - TTS：中 edge-tts zh-CN-YunxiNeural；英 kokoro-82m am_liam（本地）；BYO TTS 路径
   - 无 GPU（headless Chromium CPU）；渲染可复现（seeded 纯函数+计算式文本适配）；树莓派 5 ARM64 已验证（REMOTION_BROWSER_EXECUTABLE / kokoro_onnx / piper）
   - 样片：《RAG 与知识库》中文 4'35"（8 构建组 40min、2 轮 QC）；英文 5'02" 785 词；中文 v2 4'54" dot-field
   - 诚实面：PolyForm Noncommercial（商用需授权）、单一视觉风格（仅 star/dots 切换）、配音后文案冻结、并行构建需 ≥5GB 且 tmux pane ~12 上限、无 9:16、无 release/CI、视觉致敬抖音 @图灵宇宙（已致谢，画面自绘）

## 流程备注

- 选题来源：GitHub search API（created>09-08 stars>80 → 97 结果；created>09-05 stars>150；created>09-01 stars>300）+ HN front page。未用 web_search 深搜（README 直接取）。
- GitHub 无 token 限流 → 改用已登录 `gh api`（wuledan），稳定取 repo 详情/README/contents/issues
- 排除：wechat-intelligence-hub 2130★ 但 forks(2593)>stars → 疑似脚本刷星，弃；maskgate/ai-data-extractor/text-humanizer 均创建后数分钟即最后 push（单 commit、无 license 或被挡）→ 弃
- 三选题站内 0 覆盖（grep 验证：sol-pi/SoL-Pi 0；tokentab 0；anything2explainer 0）
- 截图脚本 scripts/screenshots-2026-09-14.mjs：6 张 direct 全成功（3 cover=GitHub repo 页 + SoL-Pi 博客页 + tokentab prices.py 页 + a2e SKILL.md 页），另下载 2 张仓库真实产物图（tokentab dashboard.png 1720x3264、a2e overview_3.jpg 2556x960）
- 全部图 PIL 验证非空白（stddev 25-76），宽 ≥1440 或 1720/2556，均 ≥800px
- 三篇正文词数 1102 / 1007 / 1010 全 ≥800；frontmatter 13 字段全齐 + dimensions 5 维校验通过；0 CJK、0 secret pattern
- 本地 build ✅ 839 pages（836 + 3），3 个新页 dist/reviews/*/index.html 均生成
- 质量自评：三篇均 Silver+；sol-pi 有 4 机制全解+研究故事+配置/存储细节+38 issue 诚实面；tokentab 有日志路径全表+缓存双计修正+读法建议+单 commit/Cursor stub 诚实面；a2e 有 9 阶段+4 确认点+时长表+平台/复现细节+许可/风格/资源诚实面

## git / 部署

- commit + push：待执行
- Vercel：待确认
- toolsdepth.com：待确认

## 小七通报

- 待执行
