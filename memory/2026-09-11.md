# 2026-09-11 — AI Tools Content Daily

## 产出 (3 articles, all Silver+, 07:07 cron 触发)

1. **okf-agent-memory Review 2026** (reviews, 7.4/10, body 1294 词) — okf-memory/okf-agent-memory 549★/34f (09-05 创建 6 天), MIT, Go。定位：git-native 持久记忆层，跑在 Google Open Knowledge Format (OKF) v0.2 上。
   - memory 存在 repo 的 `knowledge/`：普通 Markdown + 严格 YAML frontmatter；`index.md` 渐进式披露 + `log.md` 变更日志 + cross-link 图
   - 工具层：单个零依赖 Go 二进制，双身份 = CLI（validate/search/show/create/update/relate/bootstrap/init, `--strict --drift`）+ stdio MCP server（`okf mcp knowledge`）供 Claude Code/Cursor/Codex
   - 性能（项目自测表）：BM25 概念搜索 <300µs；50+ 概念 bundle parse+graph validate ~4ms；冷启 <4ms；RSS <15MB；检索成本 $0（对比 embedding 150-800ms / $0.10-0.50 per 1k）
   - 5 层架构：OKF spec → agent 约定 → agent skill → Go tooling → knowledge corpus；理念"语义推理与语法处理分离"
   - Agent Memory Convention 10 条保证：search-before-write / prefer update over dup / 不存 CoT / 保留 provenance / 保留不确定性 / 留 log / review loop / validate / **never claim persistence unless confirmed**（最戳中多数 memory 系统盲点）
   - 信任层：`generated` vs `verified`、`status`、`stale_after`、`sources`
   - 安全史：v0.1.4 symlink 隔离+保留文件保护；v0.1.5 MCP 参数消毒 path traversal/frontmatter injection/log-header injection + 对抗测试
   - 诚实面：是约定+工具而非自动捕获记忆（需自己接线）；检索是词法 BM25 非语义；性能表自测且比的是 Go 赢的轴（延迟）；6 天大的 v0.1.5，生态只有 Homebrew + 3 例 bundle
   - 角度：站内 0 覆盖；与 09-06 的 agent-memory（Claude memory runtime）不同产品

2. **geiger Review 2026** (reviews, 7.6/10, body 1205 词) — Atomburstofficial/geiger 101★/2f (09-06 创建), MIT, JS/Node 18+, npm `geiger-scan`, npx 即用。定位：AI agent 只读盘点器（"Geiger counter for AI agents"）。
   - 场景：2026-08 某开源 agent harness 3 周 0→200k★，插件生态 13k repo，一键桌面客户端同日上线，办公室都在装 —— 每装一个=能做命令执行/读文件/持凭据，藏在不复看的 dotfiles 里
   - 检测面：Claude Code（global+project MCP/hooks/plugins/skills/subagents/apiKeyHelper）、MCP hosts（Claude Desktop/Cursor/Windsurf/VS Code/Cline/Roo/Continue/Zed）、长尾 agent（Codex/Gemini/Kilo/Grok/Aider/OpenCode/Qwen/Copilot CLI/Goose/Junie/Ollama/LM Studio…）、AI 编辑器扩展、Chrome/Edge/Brave/Firefox 扩展含授权、JetBrains 设置存在性、global npm CLI（直接读不跑 npm）
   - 输出：按生态分组，每条含 what/origin/capability/evidence path；能力标签 EXECUTES / HOLDS-SECRETS / BROAD-FILESYSTEM / BROAD-WEB / NETWORK；origin=registry/store/git/local/remote/**UNKNOWN-ORIGIN**；识别 policy wrapper 并两层都报
   - 三承诺：read-only（唯一写入=你指定的 --json）；no telemetry；secrets by shape only（只报 key 名+文件+类型，永不含值，redaction pass + 测试强制）
   - **--diff baseline.json 漂移告警**：lockfile 心智——接受现状、变更即告警；--strict 只在"新增可执行/持密"时 exit 2；fleet 模式给 MSP（RMM 定时写 per-machine json）
   - 诚实面（README 自述）：只读已知 config 路径（非标准路径/其他账户/容器/WSL 看不到）；读配置非运行时行为；不判恶意（origin≠trust）；TOML 部分可解析标低置信；生态每周变，检测器刻意做小
   - 角度：站内 0 覆盖；与 agent-governance-toolkit（微软策略执行）、agentglass（可观测 dashboard）均不同——geiger 是配置层盘点

3. **browser-use-pi Review 2026** (reviews, 7.1/10, body 1232 词) — browser-use/browser-use-pi 265★/11f (09-05 创建), MIT, TypeScript。定位：browser-use 团队的 TS web agent SDK（从 @browser_use/js 改名）。
   - 核心 bet：不给定 click/type/extract 工具表，而是给 persistent V8 REPL + raw CDP，agent 自己写 JS 构建 helper；管线 task→Pi Mono→V8 REPL→raw CDP→Chrome，AX tree+screenshot 回流
   - API：BrowserUse.create / agent.run / agent.followUp / agent.close；Type(Box) schema 类型化结果 + validateResult 纠错（明确"校验形状不使事实为真"）；状态 completed/max_steps/timeout/cancelled/cost_limit/context_limit/incomplete/error；per-run maxSteps(40)/timeoutMs(300s)/maxCostUsd（软上限，可超一个 response）；hooks beforeToolCall 可 block（例：拦截 purchase）；events() async iterator + onEvent 背压
   - sessions：follow-up/保存登录/workspace 文件/cloud browser 或本地 Chrome；compaction 摘要旧上下文但不重放浏览器动作；录制+GIF+交互高亮
   - **benchmark 页最诚实**：翻出历史 Hard /106 与 V2 /100 各 SHA 分数（91 标为 unstable peak，后复跑 79/85/84），明说"不构成因果/优势/SOTA"，并给出 freeze SHA+taskID+model+budget+judge+env+retries 的验证协议
   - 诚实面：需 OPENROUTER_API_KEY + BROWSER_USE_API_KEY；quickstart 默认 cloud browser（付费）；telemetry 默认开；JS worker 有 fs+network 访问，hooks 是应用控制非沙箱；macOS 验证、Windows 未验证；Bun 需 Node on PATH；无 tagged release；benchmark 明确是历史非当前候选
   - 角度：与站内 macos-harness（macOS computer use 教程）、browser-automation comparison 不同产品——这是 app 内嵌 SDK

## 流程备注

- 选题来源：GitHub search API 多轮（created>09-08 stars>80 / >09-05 stars>150 / agent created>09-03 stars>100）+ HN front page
- **GitHub 井枯第四天**：top 里大量前几日已排除项（holo-card-studio、turnstile-bypass 家族、fanzha-ai-proxy、screenwriting-skills、NavierStokes、dream-loop/superastra/bankmcp/i-have-adhd/whiteboard-animator/pcb-skill/linkedin-agent-skill 已做）；今日新增排除：routeVSCODE 326★ 但 1 次 push 5 秒内完成（star-farm 特征）→ 弃；cek-probe-model 无 license；gongwen-gbt9704-skill 中文公文（题材偏窄）；Edge0 无描述；short-video-generator-AI 已排除过
- 三个选题均站内 0 覆盖（grep 验证：okf/okf-agent-memory 0；geiger 0；browser-use-pi 0）
- 截图脚本 scripts/screenshots-2026-09-11.mjs：6 张全 direct 成功（3 cover GitHub repo 页 + 3 二级真实页 okf-memory.dev / atomburst.io/geiger / browser-use.com，均 1440x900，206-528KB，PIL 非空白验证通过）
- 无 CJK、无 secret pattern 全部 0 命中
- 本地 build ✅ 836 pages（833 + 3 新增），frontmatter 字段全齐（schema keys 校验通过）
- 质量自评：三篇均 Silver+；okf 有 5 层架构+10 条保证+性能表+安全史+3 诚实面；geiger 有检测面全表+三承诺+baseline diff+自我局限 5 条；browser-use-pi 有 API/stop status+hooks+benchmark 自否+6 条局限

## git / 部署

- commit 8fdd0f0 pushed（a2cae84..8fdd0f0），10 files，含 3 md + 6 png + 1 script + 1 memory
- Vercel：最新 production deployment（2m 前）state=Ready，41s
- toolsdepth.com/reviews/{okf-agent-memory,geiger,browser-use-pi}-review-2026 全部 HTTP 200 + title 确认
- cover 图 https://toolsdepth.com/images/reviews/*/cover.png 全部 200（229/210/209 KB）

## ✅ 小七通报（07:2X CST，成功）

- sessions_send agentId=main **成功**（main 模型通道已恢复，连续 5 天故障结束）
- 小七已收录 + 独立抽检 toolsdepth.com/reviews/geiger-review-2026 → HTTP 200、title/h1 正确、正文 13,231 字、配图 ✅
- 小七已把 09-06~09-10 五日积压日报补记入主记忆（memory/2026-09-06/07/08/09.md），09-08 主 memory 本次新建
- 小七发现：**09-05 AI Tools 无产出**（git log 09-04→09-06 直跳，cron 疑似未触发）→ 待核实当日 cron 运行历史
- 小七另报 P0 给武总：全局默认模型通道未修复（atlas-codex/gpt-5.4 503 + open-code/deepseek-* 401），建议切回 deepseek/deepseek-v4-flash，待授权重启 gateway
