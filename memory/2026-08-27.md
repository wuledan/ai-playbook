# 2026-08-27 — AI Tools Content Daily

## 产出 (3 articles, all Silver+, 07:07 cron 触发)

1. **scroll-craft Review 2026** (reviews, 7.8/10, body 1488 词) — nateherkai/scroll-craft 1008★/164 forks（08-22 创建，5 天内），MIT，Claude Code plugin（`/plugin marketplace add nateherkai/scroll-craft`）。
   - 定位：把 interaction 与 craft 当一件事做 — scroll 即 timeline（视频逐帧 scrub、sections pin、headlines 逐行组装、page ground 变色），告别"well behaved and forgettable" vs "flashy and broken" 两难
   - 8 种互斥 page grammar（filmic one-shot/chaptered editorial/live surface/continuous world/typographic poster/gallery/split stage/rhythmic cutlist），每种禁止其他所需的模式 → 两个 build 不可能悄悄收敛成同一模板
   - Fingerprint gate：新 build 必须与历史所有页面在 6 维度（grammar/nav/hero/act shape/close/signature move）中至少 4 维不同，失败则改计划不改记录；registry 初始为空（防自我重复而非防别人）
   - 设计底线：2 字体上限、45-75ch measure、4px spacing base、6 色角色+1 accent、无纯黑、depth 五工具、feeling curve（相邻 act 同情绪=填充物）、单 engineered peak（peak-end rule 字面应用）
   - Refuse list：feature-card grids、01/06 counters、gradient text、em dash、invented statistics、fake dashboards、AI-purple gradients、cream-and-brass 工艺色板
   - 自验证：headless browser 逐 scroll 位置检查 dead scroll / 永不 full opacity 的 cue / 合成页逐行对比度（取最亮帧）/ 静默未解码的 clip（"legs stuck on a poster"），输出 contact sheet
   - Engine 永不 per-project 修改：6 色 token + 2 字体 + `--sc-p` custom property，防"同一 runtime 换皮"
   - 诚实边界：仅 Windows 实测（脚本搜 3 平台但无 Mac build）；生成视频真花钱（ten-leg world flight 是大额花费，自备素材免费）；强 opinionated 会拒绝主流 AI 页面样式
   - 角度：agent-skill 赛道首个把"设计标准本身"当产品的工具，对比 v0/Lovable（prompt luck 无底线）、Framer/Webflow AI、手写 GSAP scrollytelling

2. **doop Review 2026** (reviews, 7.7/10, body 1523 词) — kgoedecke/doop 418★/40 forks（08-22 创建，4 天内），AGPL-3.0，TypeScript，cloud doop.design。
   - Paper.design 的开源替代：多人设计画布，人与 AI agent 在同一 Canvas（`/c/<id>`）实时协同编辑 Frames（sandboxed iframe 渲染真实 HTML）
   - MCP server 16 工具：get_guide/set_status/get_feedback/list_canvases/create_canvas/get_canvas/view_website/import_webpage/create_frame/get_frame/get_frame_screenshot/set_frame_html/append_frame_html(流式)/edit_frame_html/update_frame/delete_frame；OAuth 绑定 human 身份（agent 以批准者的身份行动）
   - 工程亮点：iframe sandbox=allow-scripts + DOM-morphing in place（无 white-flash，script 重执行、样式字体不动）；typewriter reveal ~500 chars/s 平滑流式（半截 tag 丢弃、未闭合 script 切除、未闭合 style 闭合）；human 编辑取消 reveal（人类接管）
   - Doop Agent：内置设计团队（UX/copy/brand/accessibility roles 在 shared/agents.ts），queue card / @mention / feedback 触发；server 自家 ANTHROPIC_API_KEY 付 free tier（RESIDENT_TASK_LIMIT=5），之后连 ChatGPT subscription（Codex 后端，gpt-5.6-sol/terra/luna）或 OpenAI API key
   - 注意：README 自警 ChatGPT 订阅路径不符合 OpenAI 条款、重度使用可能限流/封号（CHATGPT_CONNECT_DISABLED=1 可关）；welcome demo 是 scripted（server/demo.ts）
   - Design memory：pin exemplar frames + distiller（claude-haiku-4-5）沉淀 durable style rules；feedback 变 canvas open request，下个 agent 调用时以 HUMAN FEEDBACK block 投递，claim 一次
   - 部署：docker compose up（:4400）或 npm run dev（嵌入式 PGlite 零配置）；生产需真 Postgres + BETTER_AUTH_SECRET + SMTP；AGPL-3.0 + doop 名称/logo 商标（衍生服务需 rebrand）
   - 角度：开源界首个真正的 agentic 设计工作区（live multiplayer + MCP），对比 Paper.design（闭源）、Figma AI（辅助非 agentic）、prompt-and-refresh 生成器

3. **hayamimi Review 2026** (reviews, 7.5/10, body 1328 词) — oboroge0/hayamimi 248★/15 forks（08-25 创建，2 天内），MIT，Python，sherpa-onnx。
   - "早耳" = quick ear：CPU-only 实时多语种 STT，无 GPU 无云 API <2GB RAM；说话中 partial subtitles 每 ~0.5s 更新，停口后 ~100ms 出 final line（ja）
   - 核心架构：不用单一 Whisper 接受精度上限，而是 spoken-LID（whisper-tiny 前 4s + char-set 仲裁）→ 路由到语言专家模型：ja=ReazonSpeech、zh=Paraformer-zh、ko/yue=SenseVoice small、en+24 EU=Parakeet TDT v3、~1600 语种兜底 Meta Omnilingual ASR；全部 INT8 量化 ONNX
   - 实测（docs/SCORECARD.md 端到端）：ja 广播 CER 5.8%（beam search）vs whisper-large-v3-turbo 13.8% 同剪辑 — 不到一半错误率；5 语种 LID 全对；ja final 延迟 ~100ms；5 语 soak test 均值 236ms/最大 552ms；RTF 全线 <0.2（9-16x realtime，6 核桌面 CPU）；<2GB RAM（--max-resident 2 时 1.35GB）
   - 双通道：2s silence 后 batch re-decode 二段 refine（ja 真实广播 15.5%→12.0%）；LRU 模型驱逐保内存有界
   - 实用面：--serve 起 dashboard/OBS overlay/transcript 三视图（:8833）；--input ws 让手机/ESP32 走 WebSocket 流音频（LAN）；--speakers CAM++ 说话人标签（turn-taking 非真 diarization）；--translate ja→en(FuguMT)/zh,ko(M2M-100)
   - 诚实边界：句中 code-switching 不支持（每 utterance 单语言）；hotwords 对 ja tier 无效（byte-level BPE 不兼容，用 --replace）；双人重叠不分离；翻译数值不可靠（ja→zh/ko 不保数字，金融场景别用）；mic 管线仅自测
   - License 注意：代码 MIT，但 ja→en 翻译模型 CC BY-SA 4.0（share-alike，再分发该模型权重需同许可）；模型 ~3.1GB 下载（--minimal 1.1GB ja/en）
   - 角度：本地优先隐私直播字幕（OBS + 翻译 + LAN 手机麦克风），对比本地 Whisper（同剪辑 13.8% vs 5.8%）、云 STT（音频离机）、OBS 平台字幕（云端）

## 流程备注

- 选题来源：GitHub trending（created:>2026-08-21 stars:>150）+ HN front page（今日为 AWS 收购 DuckLabs/GLM-5.3-Flash/Qwen3.8-Flash-Next 等，无强 AI 工具帖）
- 候选排除：x64dbg-mcp-server（08-25 已写）、watermark-remover（08-25 已写）、kimodo.cpp（08-26 已写）、grok-bot-0.18-reconstructed（逆向工程风险）、open-higgsfield（0 forks 无 license 存疑）
- 三个选题均站内 0 覆盖（grep 全 src/content 验证）
- 截图脚本 scripts/screenshots-2026-08-27.mjs 全部 proxy 成功（3 张 1440x900 ✅，200-213KB，PIL 亮度/方差验证非空白，GitHub repo 页）
- 无 CJK（仅 hayamimi 标题内产品本名"早耳"2 处，属工具名）、无 JSON-LD schema、无 secret pattern（sk-ant/AKIA/ghp_/hf_ 均无）
- 本地 build ✅ 802 pages（上次 799 + 3 新增）
- Commit: `4900dc4` content: AI Tools daily 2026-08-27 — 7 文件干净提交（3 md + 3 png + 1 script），push 直连成功（无需绕代理）
- Vercel: GitHub API deployment 6113568602 success（Production, sha 4900dc4，约 1 分钟内 Ready）
- 验证: toolsdepth.com 首页 + 3 篇新文章全部 HTTP 200，正文关键词确认渲染（fingerprint gate / MCP server / sherpa-onnx+5.8%+早耳）

## 质量自评

- scroll-craft: Silver+（8 grammar 互斥机制 + fingerprint gate 6 维度量化 + 设计底线细则精确 + headless 自验证 4 项检查 + engine 永不修改哲学 + 诚实 Windows-only/费用边界 + 与 AI 建站工具对比）
- doop: Silver+（16 MCP 工具表 + DOM-morphing/typewriter healing 工程论证 + Doop Agent 三层 steering 精确 + 免费层/模型 tier 量化 + AGPL/商标/OpenAI 条款风险透明 + 与 Paper.design/Figma AI 对比）
- hayamimi: Silver+（5 路路由架构图 + 实测 CER/LID/RTF/RAM 数据齐全 + 双通道 refine + OBS/WS 实用场景 + 诚实 limitations 清单 + 与 Whisper/云 STT 对比）

## 阻塞/风险

- 无阻塞。提示：scroll-craft 无独立 benchmark（仅 3 个 showcase build），且 macOS 未实测；doop 的 ChatGPT 订阅路径合规风险已在文中标注，agent 质量无量化基准；hayamimi 的 hotwords/ja 兼容问题 + Windows-only 测试面是内容短板
