# 2026-09-02 — AI Tools Content Daily

## 产出 (3 articles, all Silver+, 07:07 cron 触发)

1. **open-seo-mcp-skills Review 2026** (reviews, 7.6/10, body 1030 词) — Ryze-AI-Adgent/open-seo-mcp-skills 255★/2 forks（08-29 创建，4 天内），MIT，Shell/Claude skills。
   - 定位：Claude 的 SEO+GEO skills 包，直连**你自己的** GSC/GA4/Ads 数据（经 Ryze MCP connector），DataForSEO 内置 — 反 hosted wrapper（~28% markup）模式
   - 8 个 skill：seo-audit / keyword-research / rank-tracking / competitor-gap / backlink-check / ai-visibility / content-brief / seo-vs-ads，每个都有数据源表 + fallback 声明
   - 核心卖点：① first-party data（GSC 真实排名、GA4 真实流量，非 SERP scrape 估算）② GEO first-class（ai-visibility 读 GA4 AI-referral，ChatGPT/Perplexity/Claude/Gemini 引流归因）③ 免费 MIT + 无 markup，$120+/mo Semrush 对比
   - 安装：Claude Connectors 加 `https://connector.get-ryze.ai/mcp` + `claude plugin marketplace add`；MCP tools 命名空间 `<provider>__<tool>`，native__get_provider_docs 拉官方 API docs
   - 诚实边界：数据路径走 hosted Ryze connector（OAuth 给第三方看数据）；tool availability 随 workspace 变化；skills 是 prompt 不是产品；2 天新项目 2 forks
   - 角度：vs Semrush/Ahrefs（$120+/mo 估算数据）、OpenSEO-style wrapper（免费代码但 ~28% hosted markup）、raw GSC exports（无自动化）

2. **skill-cabinet Review 2026** (reviews, 7.4/10, body 867 词) — subsy/skill-cabinet 314★/19 forks（08-31 创建，2 天内），MIT，JS/Node。
   - 定位：本机 agent skills 的本地目录管理 — 扫描 `.agents`/`.claude`/`.codex`/`.cursor`（含 plugins）等 drawer，`npx skill-cabinet` 起 localhost:3781 浏览器 UI
   - 功能：按 drawer 过滤 + 名称/描述/路径/origin/copies/frontmatter 搜索；symlink 卡过滤（all/only/hide）；risk 过滤（all/elevated/hide）；读 skill body（rendered/source）+ YAML frontmatter + 附加文件；GitHub origin 跟随（inferred 标注）；跨 drawer 重复副本识别；批量删除
   - 安全模型：删除不可撤销；symlink 删除只 unlink 保 target；builtin Cursor skills 和 plugin-cache 副本会被明确警告"工具下次更新会复活"
   - 键盘流：j/k 移动、/ 查找、x 标记、d 删除；Theme 菜单（Carbon 默认）；只绑 127.0.0.1
   - 诚实边界：只读+删，无 install/update/versioning；static risk 是启发式（regex 模式），非安全审计；单机无云同步；origin inference best-effort；2 天新项目 19 forks
   - 角度：vs 文件管理器翻 ~/.claude/skills（无搜索无去重无 risk 提示）、skill marketplace（只装不管）

3. **course2md Review 2026** (reviews, 7.8/10, body 876 词) — mizorewww/course2md 187★/7 forks（08-31 创建，2 天内），MIT，Rust。
   - 定位：YouTube/Bilibili/本地课程会议录像 → 幻灯片插图的 Markdown/HTML 讲义，一条命令
   - 管道：下载 → SSIM 幻灯片检测（similarity 0.85 / sample 1s / cooldown 10s / ROI）→ ASR 转写 → 时间戳对齐讲义；输出 course.md/html + structured.json + frames/ keyframes + timeline.jsonl + run.json provenance
   - 5 个 ASR backend：coreml（Apple Silicon ANE，Qwen3-ASR 0.6B 或 Whisper turbo，零外部依赖，~375J/3min）/ gpu（llama.cpp Qwen3-ASR 1.7B Q8，13s）/ cpu / api（OpenRouter ~$0.000035/s）/ npu（Intel Core Ultra OpenVINO，>6x CPU，550MB）
   - 基准故事：同一 CS 讲座 Qwen3-ASR 1.7B 完美转写 NeoVim/Altair 8800/ICQ/Codex，Whisper turbo 误听（NeoWim/"PCG RTIR 8800"），Whisper Tiny 幻觉（NeoVim→"cow smell"）
   - LLM 层：proofread（20 段 batch、temp 0、失败自动 fallback 原始 ASR）+ summarize（TL;DR/时间戳大纲、map-reduce、幻觉防护）
   - 安装：Homebrew tap（macOS 15+ Apple Silicon 全自动）/ AUR / winget / cargo；`course2md doctor` 环境检查；`course2md remove` 清凭据
   - 诚实边界：Linux/Windows 是 developer-grade（要 cmake 编 llama.cpp）；首跑下 1-2GB 模型；api 模式音频上传云端（frames/OCR/VAD 留本地）；2 天新项目 7 forks
   - 角度：vs 平台字幕（无幻灯片）、通用 ASR 工具（无 slide detection 无讲义组装）、付费转写订阅（本地免费）

## 流程备注

- 选题来源：GitHub search API（created:>2026-08-28 stars:>30）+ HN front page（今日无强 AI 工具帖：AnkiDroid/transformer ARC 博客/Zitron 预测回顾/Codex LibreOffice 新闻）
- 候选排除：infinite-livestream（需 8x B200 + Reactor 平台，太 infra 向）、forward-implementation-first（agent 反文书化 skill，话题偏窄）、TetreesAgent_EX（"zero GPU 训练并交易" 类，疑 crypto 灰产）、DLSS5 系列/steam2/security 0day（非 AI 工具品类）
- 三个选题均站内 0 覆盖（grep 全 src/content 验证）
- 截图脚本 scripts/screenshots-2026-09-02.mjs 全部 proxy 成功（3 张 1440x900 ✅，195-403KB，PIL 亮度/方差验证非空白，GitHub repo 页）
- 无 CJK、无 JSON-LD schema、无 secret pattern（hf_/AKIA/ghp_/sk- 全部 0 命中）
- 本地 build ✅ 811 pages（上次 808 + 3 新增）
- Commit: `5cb8102` content: AI Tools daily 2026-09-02 — 7 文件干净提交（3 md + 3 png + 1 script），push 直连成功（b2d73ef..5cb8102）
- Vercel: `vercel list --prod` 最新 Production Ready（2m 前，部署耗时 36s，https://ai-tools-review-irpn3h1p0-wuledans-projects.vercel.app）；GitHub API 限流改用 CLI 确认
- 验证: toolsdepth.com 首页 + 3 篇新文章全部 HTTP 200，正文关键词确认渲染（Eight ready-made skills / drawers+npx skill-cabinet / SSIM+Bilibili）

## 质量自评

- open-seo-mcp-skills: Silver+（8 skills 数据源表 + first-party data 架构 + GEO first-class + 定价模式对比表 + MCP namespace 机制 + fallback 设计 + 与 Semrush/Ahrefs/wrapper 对比 + hosted connector 信任风险诚实标注）
- skill-cabinet: Silver+（drawer 模型 + 搜索/过滤面 + 删除安全模型含 symlink unlink 与 plugin-cache 复活警告 + static risk 启发式边界 + 与文件管理器对比）
- course2md: Silver+（5 backend 矩阵 + powermetrics 能耗基准 + Qwen3-ASR vs Whisper 精确误听案例 + SSIM slide detection 参数 + LLM proofread/summarize 容错设计 + 跨平台安装路径 + 隐私边界标注）

## 阻塞/风险

- 无阻塞。提示：open-seo-mcp-skills 数据路径依赖 hosted Ryze connector（第三方 OAuth 读数据）；skill-cabinet 删除无 undo；course2md Linux/Windows 安装 developer-grade
