# 2026-08-31 — AI Tools Content Daily

## 产出 (3 articles, all Silver+, 09:24 cron 触发)

1. **PRAXIST Review 2026** (reviews, 7.8/10, body 1203 词) — sapientinc/PRAXIST 4531★/365 forks（08-27 创建，4 天内），Fair Source License 1.0，Python 3.11+，arXiv:2608.25955。
   - 定位：autonomous research system for measurable, computer-executable research — 把"already-runnable + measurable objective"项目变成持续 evidence-driven research run（parallel peers → task-owned evaluation → durable evidence lanes → planning panel 合成下一代 agenda）
   - 分工：PRAXIST 拥有 orchestration/lifecycle/evidence protocols/replay/scheduling；task project 拥有 objective/code/evaluator/metrics/baselines（零 task-specific 假设）
   - 防局部最优：Quality-Diversity (QD) allocator + 可选 Deep Innovation Gate (DIG)；evidence lanes 三态 incubator → frontier → Gems
   - 信任模型：preregistration（run 前定 metrics/eval/baseline/threshold）+ consistent evaluation（同一 evaluator，invalid 结果剔除）+ end-to-end provenance；明确"不保证 metric 提升"，负结果也交付 evidence package + audit report + redirect 建议
   - 操作：Codex-native mode（复用 Codex 登录，无需 API key）或自备 open-source model API；9 个 bundled skills（$praxist-takeover / control / diagnostic / scientific-research / terminal-line-plot 等）；CLI：status --json / --monitor --latest / stop / resume；Ctrl-C 只关 monitor
   - 示例：rocket_booster_recovery（Python/JAX 与 Rust 双实现）；Linux release-tested，macOS 仅 compatibility target（先跑 praxist doctor）
   - License：Fair Source 1.0（source-available，<$1M 年收入免费商用，≥$1M 需与 Sapient Intelligence Pte Ltd 谈商业许可；高校/非营利学术豁免）；不采集实验数据，key masked 输入
   - 诚实边界：无公开 end-to-end benchmark 数据（靠 arXiv paper + 架构）、0.5.0 太新、task harness contract 是真实工程量
   - 角度：vs AutoML（固定搜索空间调参 vs 全研究循环）、MLAgentBench（benchmark 评测）、人工迭代

2. **codex-with-chatgpt Review 2026** (reviews, 7.7/10, body 1134 词) — XiaoDuoYa/codex-with-chatgpt 1407★/155 forks（08-28 创建，3 天内），MIT，TypeScript，Node ≥20。
   - 定位：ChatGPT 网页版订阅（Plus/Pro web quota 闲置）当 planning/review 大脑，Codex 保留全部执行权；无 API key、无逆向代理
   - 双平面架构：Control plane（Computer Use，tiny [C2C] state msgs INIT→PLAN→EXECUTED→REVIEW→DONE，<1KB，从不贴 diff/log/文件体）+ Data plane（8 个只读 MCP tools：workspace_info/list_directory/read_file/search_workspace/git_status/git_diff/test_status/execution_summary）
   - 安全模型：read-only by construction（write/shell/commit 工具根本不存在）；OAuth 2.1（PKCE S256 + DCR + rotating refresh tokens）；one-time pairing code（CSPRNG，5min TTL，5 次尝试，限速，用完销毁）；one workspace = one boundary（canonical realpath 防 symlink/../ 逃逸，有测试）；.env*/keys/SSH 默认拒绝；知道 URL 无 token=401，错 workspace=403
   - 独立 review：Codex 执行后 ChatGPT 通过 MCP 看真实 git diff + test records，不轻信 "all tests passed"
   - 76 vitest tests（path security/OAuth/pairing/MCP e2e）；c2c setup/status/doctor/pair/unpair/logs/stop；cloudflared 自动装
   - 一键安装：给非技术用户的一段话（Codex 全自动 clone/build/skill 安装/browser pairing，只在 ChatGPT/Cloudflare 登录时打断）；skill 每日自查 GitHub 自动更新
   - 诚实边界：unofficial（README 明示不 affiliated/endorsed by OpenAI，web ChatGPT 走 Computer Use 是 ToS 灰色地带，重度使用有限流/封号风险）；必须有 Plus/Pro 订阅；public tunnel 默认存在（临时 URL 重启会变，稳定 hostname 需 Cloudflare 账号+域名）；ChatGPT 不能跑命令，planning 质量依赖 Codex 汇报保真；V1 太新
   - 角度：vs 原生 Codex（烧 paid tokens 做规划）、Claude Code+ChatGPT bridge、API gateway（OpenRouter/LiteLLM 按 token 收费）

3. **sepia Review 2026** (reviews, 7.6/10, body 989 词) — Nanako0129/sepia 918★/53 forks（08-28 创建，3 天内），MIT，Agent Skills 标准。
   - 定位：在"真正出卖 AI 的层"做 De-AI writing — narrative architecture 而非字词；Claude Code/Codex/Grok Build/Antigravity 四平台 portable
   - 证据：StoryScope（arXiv:2604.03136，61,608 stories，human+5 frontier LLMs）— 仅 narrative-structure features 就能 93.2% macro-F1 检出 AI 小说，surface style 编辑几乎不动（95.5%→93.9%）；tells：narrator 解释主题/单线因果 plot/情绪只做 bodily sensation/无 real-world references/线性时间/growth-and-acceptance 结局
   - 三 pass 协议：1 narrative architecture（stop explaining theme、loosen causal chain、back-load revelations、mix emotion modes、sparse character networks、name real things）→ 2 discourse flow（de-template paragraph-question、fix mid-story sag、vary rhythm）→ 3 surface style（clichés/syntax/vocabulary/register）；30-feature diagnosis rubric + per-model fingerprints（Claude/GPT/Gemini/DeepSeek/Kimi）
   - 专业文档独立规则：release notes（user impact first, artifacts per claim）、PR/issue replies（answer first, cite file:line, no reflex praise, length ∝ stakes）、postmortems（blameless toward people, merciless toward mechanisms）、tickets（title=outcome, testable acceptance criteria）、technical articles（one real dead end, one committed opinion, numbers with conditions）
   - 核心原则："calibrate to the human distribution, don't invert the AI one" — 每篇只选 3-5 moves 留 slack，避免过度修正形成新指纹；目标不是 detector evasion
   - 4 operations：write / review（只诊断）/ refactor（最小改动）/ recreate（从事实重写）；research/ 目录消化 12+ 论文（LAMP/NarraBench/Echoes in AI/QUDsim 等）
   - 诚实边界：价值受宿主 agent 写作能力上限约束；rubric 应用需判断力；fingerprint 是启发式非实测；README.zh-TW 非英文文档仅繁中
   - 角度：vs 传统 humanizer（Undetectable AI 等只改 surface 且目标过检测）、Refactoring UI skill（设计域）

## 流程备注

- 选题来源：GitHub trending API（created:>2026-08-25 stars:>150）+ HN front page（今日无强 AI 工具帖，全是 kernel/security/retro）
- 候选排除：sepia/headcount 词面命中系误报（sepia=ideogram 文章色名、headcount=商业词汇）；open-higgsfield（NO-LICENSE 再次排除）；trustmebro（guardrail 绕过，内容风险）；my-free-code/opengrok/fire-your-seo-agency/refactoring-ui-skill/Hy4-preview/tokentab 均未选（tokentab 与 cursor-cost-tracking tutorial + honey-for-devs 主题重叠，备选）
- 三个选题均站内 0 覆盖（grep 全 src/content 验证）
- 截图脚本 scripts/screenshots-2026-08-31.mjs 全部 proxy 成功（3 张 1440x900 ✅，183-220KB，PIL 亮度/方差验证非空白，GitHub repo 页）
- 无 CJK、无 JSON-LD schema；sk- pattern 命中为假阳性（praxist-task-initialization 工具名），无真实 secret
- 本地 build ✅ 805 pages（上次 802 + 3 新增）
- Commit: `66d9673` content: AI Tools daily 2026-08-31 — 7 文件干净提交（3 md + 3 png + 1 script），push 直连成功（无需绕代理）
- Vercel: GitHub API deployment 6173605951 success（Production, sha 66d9673，约 3 分钟内 Ready）
- 验证: toolsdepth.com 首页 + 3 篇新文章全部 HTTP 200，正文关键词确认渲染（Fair Source License 1.0 / Read-only by construction / 93.2% macro-F1）

## 质量自评

- PRAXIST: Silver+（ownership 分工表 + 三态 evidence lanes + QD/DIG 防局部最优 + preregistration/consistent eval/provenance 信任模型 + Fair Source 收入阈值精确 + 负结果 evidence package + 与 AutoML/MLAgentBench 对比）
- codex-with-chatgpt: Silver+（双平面架构图 + 8 MCP 工具表 + OAuth 2.1/PKCE/pairing 安全细节精确 + 76 tests + 独立 review loop + ToS 灰色地带/隧道/延迟诚实边界 + 与原生 Codex/API gateway 对比）
- sepia: Silver+（StoryScope 93.2% 数据 + 三 pass 协议表 + 30-feature rubric + 5 类专业文档规则 + calibration 原则 + 12+ 论文证据库 + 与 humanizer 对比）

## 阻塞/风险

- 无阻塞。提示：PRAXIST 无公开 end-to-end benchmark（仅 arXiv paper），0.5.0 版本过新，Fair Source 上限挡部分商业团队；codex-with-chatgpt 的 ChatGPT web Computer Use 路径是 ToS 灰色地带（已在文中标注）；sepia 的 fingerprint 修正为启发式未逐模型实测
